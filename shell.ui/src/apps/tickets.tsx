/**
 * Placeholder for the tickets app (M1 item 11). It exists now so the registry has a real module
 * to lazy-load, which is what the entitlement-gating tests actually exercise.
 */
export default function TicketsApp() {
  return (
    <main>
      <h1>Tickets</h1>
      <p>No tickets yet.</p>
    </main>
  )
}
